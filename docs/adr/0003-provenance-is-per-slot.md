# Provenance is per-slot; SEND does not promote

The product’s claim is honesty, not “an LLM filled the form.” A single SEND that rewrites Estimated and Inferred to Confirmed is silent promotion: control cannot see what was guessed.

Each Slot carries one tag. SEND means emit the Message as shown, mixed tags included. Only an explicit per-slot accept or edit may set Confirmed.
